# mmjs-plugin utils

- Install
  ```shell
  pnpm add mmjs-plugin
  ```

- tsconfig.json
  - （如果用 Vite/Webpack 等打包工具）
  ```json
  {
   "compilerOptions": {
       "moduleResolution": "Bundler"
   }
  }
  ```

## 近期更新
